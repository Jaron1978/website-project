# Website source

Place the reviewed production frontend source here when the GitHub repository is created.

Before publishing, check for:

- Secrets or credentials.
- Account-specific values that should not be public.
- Old/deprecated copies of pages.
- Temporary files and local build artefacts.

A public API endpoint used by browser JavaScript is not itself a secret, but the backend must still enforce appropriate permissions and validation.
