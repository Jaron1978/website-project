# Backend source

This folder is reserved for the visitor-counter backend source and related deployment files.

Suggested eventual structure:

```text
backend/
├── lambda_function.py
├── tests/
└── infrastructure/
```

Only add tests or Infrastructure-as-Code files once they genuinely exist. The repository should reflect the implemented system rather than presenting planned Cloud Resume Challenge steps as completed work.
